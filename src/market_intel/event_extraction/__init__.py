"""Event extraction.

LLM-assisted extraction of `extracted_facts` and `source_claims` from
Documents, via the local LLM client (Ollama, see ADR-002). This is one of
the two component boundaries permitted to reach external systems/the LLM.
"""
